#include<stdio.h>


int main(){
    char line[100];
    scanf("%s",&line);
    
    for (int i = 0; i != '\0'; i++) 
    {
        if (line[i]=='a' || line[i]=='e' || line[i]=='i'
        || line[i]=='o' || line[i]=='u')
        {
            /* code */
        }
        
    }
    
    return 0;
}